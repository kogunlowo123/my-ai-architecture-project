# Global
Org-level, one-time resources: Terraform state bucket + DynamoDB lock table,
GitHub OIDC provider, KMS keys for audit-chain and MCP schema signing,
central CloudTrail. Applied once per account family, outside the env pipelines.
