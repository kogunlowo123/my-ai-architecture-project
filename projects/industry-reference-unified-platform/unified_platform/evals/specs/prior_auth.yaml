agent: prior_auth
current_tier: 2
golden_set: evals/golden/prior_auth_v3.jsonl   # synthetic cases only
thresholds:
  recommendation_accuracy: 0.97       # vs. licensed-reviewer gold labels
  citation_validity: 1.00             # every citation must resolve to a real policy
  reviewer_override_rate_30d: "< 0.05"
promotion_blockers:
  - any auto-denial pathway detected  # permanent
re_eval_on: [model_version, prompt_version, tool_schema_version]
