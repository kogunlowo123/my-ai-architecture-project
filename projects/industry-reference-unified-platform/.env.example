# Copy to .env — never commit real values
AWS_REGION=us-east-1
PLATFORM_ENV=dev
ANTHROPIC_API_KEY=            # injected via secrets manager in real envs
FHIR_GATEWAY_URL=https://fhir-gw.internal:8443
TAX_ENGINE_URL=https://tax-engine.internal:8443
AUDIT_KMS_KEY_ALIAS=alias/audit-chain-signing
OIDC_ISSUER=https://token.actions.githubusercontent.com
