# dev: IdP tenant + app groups, session TTL ceilings, MFA class per app risk, JIT approver count
session_ttl_max_minutes = 60
mfa_class_admin         = "phishing-resistant"
jit_approver_count      = 1
