name: nightly
on: {schedule: [{cron: '0 6 * * *'}]}
jobs:
  full-suite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo 'full eval suite + red-team corpus + dependency drift'
