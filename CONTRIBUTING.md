# Contributing

Branch model: trunk-based, short-lived feature branches. Commits: `feat|fix|infra|docs(scope): message`. Every prompt/retrieval/registry change must pass the eval gate (`.github/workflows/evals.yml`). PRs touching `identity/` or `security/` need two owners (CODEOWNERS).
